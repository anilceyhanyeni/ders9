import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <section style={ {border:"3px solid black", padding: "1rem" } } className="container">
    <p className="kirmizi-yazi">App 1</p>
  </section>
);

